<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:t="clr-namespace:TreeView.Maui.Controls;assembly=TreeView.Maui"
            x:Class="ExampleBook.Tooling.Maui.Views.DesktopMainPage"
            Title="Example Book">

    <Grid WidthRequest="1000">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="400"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>

        <ScrollView Grid.Column="0"
                    Grid.Row="0"
                    Grid.RowSpan="3"
                    BackgroundColor="DeepSkyBlue">
            <t:TreeView ItemsSource="{Binding Nodes}">
                <t:TreeView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Padding="8,8">
                            <HorizontalStackLayout.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnExampleTapped"
                                                        NumberOfTapsRequired="1" />
                            </HorizontalStackLayout.GestureRecognizers>
                            <Label Text="{Binding Name}" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </t:TreeView.ItemTemplate>
            </t:TreeView>
        </ScrollView>

        <ContentView x:Name="ExampleWrapper"
                        Grid.Column="1"
                        Grid.Row="0"
                        BackgroundColor="Black" />

        <HorizontalStackLayout Grid.Column="1" Grid.Row="1" BackgroundColor="Gray">
            <Button Text="Properties"></Button>
        </HorizontalStackLayout>
         <ScrollView Grid.Column="1" Grid.Row="2" BackgroundColor="Blue">
            <CollectionView ItemsSource="{Binding SelectedItemsProperties}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout>
                            <Label Text="{Binding Name}" TextColor="Black"/>
                            <Label Text="{Binding Value}" TextColor="Black"/>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>
    </Grid>

</ContentPage>
